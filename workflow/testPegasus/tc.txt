tr world {
    site local {
        pfn "/usr/bin/cat"
        arch "x86_64"
        os "linux"
        type "INSTALLED"
    }
}

tr hello {
    site local {
        pfn "/usr/bin/cat"
        arch "x86_64"
        os "linux"
        type "INSTALLED"
    }
}

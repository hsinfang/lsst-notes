tr world {
    site lsstvc {
        pfn "/usr/bin/cat"
        arch "x86_64"
        os "linux"
        type "INSTALLED"
    }
}

tr hello {
    site lsstvc {
        pfn "/usr/bin/cat"
        arch "x86_64"
        os "linux"
        type "INSTALLED"
    }
}
